name: ${name}
version: ${version}

mains:
  papermc: "${group}.extension.defaults.papermc.Default"
  spigotmc: "${group}.extension.defaults.spigotmc.Default"
  foliamc: "${group}.extension.defaults.foliamc.Default"

git:
  provider: github
  owner: ${git_org_name}
  repository: ${git_org_repository}
